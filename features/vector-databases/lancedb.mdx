---
title: "LanceDB"
description: "LanceDB as the default local vector database backend."
---

{/*
Source: ahvn.utils.vdb — LanceDB is the default vector backend. Used in the legacy 5-min quickstart for VectorKLStore.

Planned sections:
1. Default usage — where LanceDB is used automatically
2. Connection setup — provider="lancedb", uri path
3. Indexing and querying
4. Local development and prototyping
*/}

LanceDB is the default vector database backend in AgentHeaven, offering zero-configuration local storage for embeddings and semantic search.

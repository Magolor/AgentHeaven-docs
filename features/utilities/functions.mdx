---
title: "Functions"
description: "Function introspection, schema extraction, docstring parsing, and synthesis."
---

{/*
Source: ahvn.utils.basic.func_utils

Planned sections:
1. func2meta — extract function metadata (name, module, qualname)
2. func2schema — generate JSON schema from function signature
3. code2func — convert code string to a callable function
4. parse_docstring — parse Google-style docstrings
5. sig2func — create a function from a signature spec
6. synthesize_docstring/def/signature — generate function specs programmatically
*/}

The `func_utils` module provides function introspection and synthesis tools, heavily used by `Capsule` and `ToolSpec` for converting between live Python functions, code strings, and structured schemas.

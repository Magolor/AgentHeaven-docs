---
title: "Serialization"
description: "Load, save, and convert data across formats: JSON, YAML, pickle, hex, base64, and more."
---

{/*
Source: ahvn.utils.basic.serialize_utils

Planned sections:
1. load/save — file-based I/O for txt, yaml, pkl, hex, b64, json, jsonl
2. dumps/loads — in-memory serialization for the same formats
3. serialize/deserialize_path — path-safe serialization
4. serialize/deserialize_func — function serialization (used by Capsule)
5. AhvnJsonEncoder/AhvnJsonDecoder — custom JSON codec
6. heal_json — LLM-friendly JSON repair
*/}

The `serialize_utils` module provides unified load/save/dumps/loads functions across all common data formats, plus specialized serializers for paths and functions.

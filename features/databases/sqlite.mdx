---
title: "SQLite"
description: "SQLite setup and usage as the default local database backend."
---

{/*
Source: ahvn.utils.db — SQLite is the default backend for ConfigManager, caching (DatabaseCache), and local development.

Planned sections:
1. Default usage — where SQLite is used automatically (config.db, cache DBs)
2. Connection setup — provider="sqlite", database path
3. Schema inspection — SchemaIndex with SQLite
4. Limitations and when to upgrade to PostgreSQL
*/}

SQLite is the default database backend in AgentHeaven, used for configuration storage (`~/.ahvn/config.db`), `DatabaseCache`, and local development.

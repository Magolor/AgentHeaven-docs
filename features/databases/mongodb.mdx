---
title: "MongoDB"
description: "MongoDB integration for document-oriented storage."
---

{/*
Source: ahvn.utils.mdb (MongoDatabase, MongoCompiler), ahvn.klstore.MongoKLStore, ahvn.klengine.MongoKLEngine

Planned sections:
1. MongoDatabase abstraction — connection, collections, CRUD operations
2. MongoCompiler — compile queries from structured inputs
3. MongoKLStore — knowledge persistence in MongoDB
4. MongoKLEngine — knowledge retrieval from MongoDB
5. Connection setup — URI, authentication, replica sets
*/}

AgentHeaven integrates with MongoDB through `pymongo`/`motor`, providing a document-oriented storage backend used for both general data storage and knowledge persistence via `MongoKLStore`.
